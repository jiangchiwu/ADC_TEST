# OpenOCD configuration for STM32H750 with ST-Link

source [find interface/stlink.cfg]

transport select swd

adapter speed 4000

source [find target/stm32h7x.cfg]

reset_config srst_only

init
echo "Target initialized successfully!"